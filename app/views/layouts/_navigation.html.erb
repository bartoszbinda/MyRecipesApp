<header class="navbar navbar-fixed-top navbar-default">
    <div class="container">
      
      <%= link_to "Cafeteria Ordering System", "/home", class: "btn btn-lg btn-success", id: "logo" %>
      <div class="nav_item">
        <%= link_to "Menu", "/meals", class: "btn btn-default btn-warning" %>
      </div>
      <div class="nav_item">
        <%= link_to "CookPage", "/cookpage", class: "btn btn-default btn-warning" %>
      </div>

<% if user_signed_in? %>
      <div class="nav_item">
  <%= link_to 'Edit profile', edit_user_registration_path, :class => 'btn btn-warning' %> |
         </div>
         <div class="nav_item">
  <%= link_to "Logout", destroy_user_session_path, method: :delete, :class => 'btn btn-warning' %>
         </div>
         <% else %>
         <div class="nav_item">
  <%= link_to "Sign up", new_user_registration_path, :class => 'btn btn-warning'  %> |
         </div>
         <div  class="nav_item">
     <%= link_to "Login", new_user_session_path, :class => 'btn btn-warning' %>


         </div>
<% end %>

      </div>
    </div>
</header>